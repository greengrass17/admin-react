import {
  REQUEST_USER,
  RECEIVE_USER
} from '../actions';

const initialState = {
  user: {},
  // groups: []
}

function mercuryApp(state = initialState, action) {
  return state
}

export default mercuryApp;
